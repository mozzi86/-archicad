---
phase: 04-recipes-reihe-2-komplexere-elemente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-recipes-reihe-2-komplexere-elemente/SCHEMAS.md
  - .planning/phases/04-recipes-reihe-2-komplexere-elemente/TEST-SET.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "SCHEMAS.md enthält vollständige Schemata für CurtainWall + 5 Sub-Element-Typen + Library-Object"
    - "TEST-SET.md dokumentiert Beispiel-GUIDs aus Teamwork (port 19724) für Wave-2-Subagent-Konsumierung"
    - "Keine Schreib-Operation gegen das Teamwork-Projekt"
  artifacts:
    - "SCHEMAS.md"
    - "TEST-SET.md"
---

<objective>
Wave-1-Orchestrator-Setup gegen Teamwork-Real-Projekt (port 19724, Futurelab_BI_Umbau_ARZ_OBL_V29). Discovery + Listing aller relevanten Element-Typen, Schema-Sammlung für die 2 Recipe-Subagents in Wave 2.

**Strikt Read-Only.** Keine Create/Update/Delete-Calls im Real-Projekt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-recipes-reihe-2-komplexere-elemente/04-CONTEXT.md
@reference/mcp-conventions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discovery — alle CurtainWall-Sub-Element-Typen + Library-Object</name>
  <action>
Discovery-Queries für alle 7 Element-Typen, parallel:
- „get all curtain walls"
- „get curtain wall segments"
- „get curtain wall frames"
- „get curtain wall panels"
- „get curtain wall junctions"
- „get curtain wall accessories"
- „get library objects placed"

Plus: Discovery für Detail-Tools pro Sub-Element-Typ + Property-Lookup-Tools.

Verifikation der `elements_get_elements_by_type`-Enum-Werte aus Phase 2 reicht meist — alle 6 CurtainWall-Subs + Object sind im Enum vorhanden.
  </action>
  <verify>Discovery liefert Tool-Namen + Schemas für jeden Element-Typ.</verify>
  <acceptance_criteria>
    - "Schema-Definitionen aller 7 Element-Typen erfasst"
    - "Detail-Tools identifiziert (vermutlich `elements_get_details_of_elements` mit typ-spezifischen Details — aber Vorsicht: get_details ist in AC29 für viele Typen broken)"
  </acceptance_criteria>
  <done>Discovery-Schemata bekannt.</done>
</task>

<task type="auto">
  <name>Task 2: Listing in Teamwork-Projekt — alle 7 Element-Typen</name>
  <action>
Pro Element-Typ ein `elements_get_elements_by_type`-Call gegen `port: 19724`:
- `CurtainWall`
- `CurtainWallSegment`
- `CurtainWallFrame`
- `CurtainWallPanel`
- `CurtainWallJunction`
- `CurtainWallAccessory`
- `Object`

Jeweils 1-3 GUIDs pro Typ als Sample notieren. Falls einer leer (z. B. kein CurtainWallAccessory im Projekt) — dokumentieren als „im aktuellen Projekt nicht vorhanden — Schema-only-Recipe-Section".

Für `Object` zusätzlich: einige Beispiele pro Subtype-Pfad sammeln. Dazu nach erster Liste:
- `properties_get_all_property_ids_of_elements` für 5-10 Object-GUIDs.
- Property-Werte lesen, vor allem Built-in „Subtype" / „Library-Part-Name".
- Subtypes gruppieren (z. B. 3-5 charakteristische Pfade).
  </action>
  <verify>Listing-Calls liefern GUIDs ohne Fehler.</verify>
  <acceptance_criteria>
    - "Pro Element-Typ ≥1 Sample-GUID, ODER explizit ‚nicht vorhanden' dokumentiert"
    - "Object-Subtypes: 3-5 charakteristische Library-Pfade identifiziert"
  </acceptance_criteria>
  <done>GUID-Map für TEST-SET.md vollständig.</done>
</task>

<task type="auto">
  <name>Task 3: Schema-Sammlung pro Element-Typ</name>
  <action>
Pro Typ folgendes in SCHEMAS.md festhalten:

**CurtainWall (Top-Level):**
- Read: via `elements_get_elements_by_type` + Property-Workaround (get_details vermutlich broken)
- Update: Top-Level-Settings (Polygon, Höhe, Profil)
- Classify: standard
- Delete: standard + Hosted-Pre-Check (Sub-Elemente sterben mit)

**CurtainWallSegment / Frame / Panel / Junction / Accessory:**
- Read pro Sub-Typ: via `elements_get_elements_by_type` mit Subtype-Enum-Wert
- Hierarchie-Beziehung: wie findet man Sub-Elemente eines Top-Level-CurtainWall? `elements_get_connected_elements`-Pattern probieren.
- Update: typ-spezifisch (Panel-Material, Frame-Profil, Junction-Verbindungs-Typ)
- Sub-Element-Delete oft sinnlos — sterben mit Top-Level.

**Library-Object:**
- Read: standard + Subtype-Property-Read
- Create: `elements_create_objects` (siehe Phase-3 SCHEMAS — verifiziert), aber NICHT in Phase 4 ausgeführt
- Update: typeSpecificDetails (Größe, Position, Rotation) + GDL-Parameter
- Subtype-Identifikation: Built-in „library-part-name" Property

Format wie Phase-3 SCHEMAS.md.
  </action>
  <verify>SCHEMAS.md hat alle 7 Element-Typen mit Read/Update/Delete-Sektionen.</verify>
  <acceptance_criteria>
    - "Schema-Definitionen mit Pseudocode-Snippets pro Element-Typ"
    - "Hierarchie-Beziehung CurtainWall ↔ Sub-Elemente erklärt"
    - "Subtype-Discovery-Pattern für Library-Objects dokumentiert"
    - "Bug-Hinweise (get_details-Bug, gdl_parameters-Bug) wo relevant verlinkt"
  </acceptance_criteria>
  <done>SCHEMAS.md Subagent-ready.</done>
</task>

<task type="auto">
  <name>Task 4: TEST-SET.md schreiben</name>
  <action>
TEST-SET.md mit Phase-4-Run-spezifischen GUIDs aus Teamwork:

```markdown
# Phase 4 Test-Set

**Quelle:** Teamwork-Projekt Futurelab_BI_Umbau_ARZ_OBL_V29 (port 19724), Read-Only-Discovery.
**Hinweis:** GUIDs sind Real-Projekt-spezifisch. In Recipe-Worked-Examples als `<placeholder-guid>` referenzieren, nicht hartkodieren. Diese Datei dient nur als Wave-2-Subagent-Input.

| Element-Typ | Beispiel-GUID | Notiz |
|---|---|---|
| CurtainWall | <guid> | ... |
| CurtainWallSegment | <guid> | ... |
... |
| Object | <guid> | Subtype: ... |
```

Bei Sub-Elementen: nach Möglichkeit die Sub-GUIDs eines bestimmten Top-Level-CurtainWalls auflisten, damit die Hierarchie im Recipe-Beispiel funktioniert.
  </action>
  <verify>TEST-SET.md komplett mit allen 7 Element-Typen.</verify>
  <acceptance_criteria>
    - "Alle 7 Element-Typen mit Sample-GUIDs oder ‚nicht vorhanden'-Markierung"
    - "Hinweis zur Placeholder-Konvention für Recipes"
  </acceptance_criteria>
  <done>TEST-SET.md committed.</done>
</task>

</tasks>

<verification>
- [ ] Discovery für alle 7 Element-Typen.
- [ ] Listing-Calls für alle 7 in Teamwork.
- [ ] SCHEMAS.md mit kompletten Schemata.
- [ ] TEST-SET.md mit Sample-GUIDs.
- [ ] Keine Schreib-Operation im Teamwork.
</verification>

<success_criteria>
- Wave-2-Subagents haben alles, was sie zum Recipe-Schreiben brauchen.
- Read-Only-Constraint eingehalten.
</success_criteria>
