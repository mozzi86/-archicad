---
phase: 04-recipes-reihe-2-komplexere-elemente
plan: 04
type: execute
wave: 3
depends_on:
  - 04-02
  - 04-03
files_modified:
  - SKILL.md
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "Beide neuen Recipes erfüllen Konsistenz-Checks (TOC, voll-qualifizierte MCP-Toolnamen, keine nested References)"
    - "Live-Sample-Validation pro Recipe gegen Teamwork-Port 19724 erfolgreich"
    - "SKILL.md Recipe-Index Hinweis-Text aktualisiert falls nötig"
---

<objective>
Wave-3-Validation für die 2 neuen Recipes (curtain-walls.md + library-objects.md).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Konsistenz-Checks für beide Recipes</name>
  <action>
Bash-Check:
```bash
SK=/Users/ap/.claude/skills/archicad
for f in $SK/recipes/curtain-walls.md $SK/recipes/library-objects.md; do
  echo "=== $f ==="
  wc -l "$f"
  head -25 "$f" | grep -q "Inhaltsverzeichnis" && echo "  TOC: OK" || echo "  TOC: FAIL"
  grep -c "mcp__archicad__" "$f"
  grep -E '(^|[^_])archicad_(call_tool|discover_tools|list_active_archicads)' "$f" && echo "  UNQUALIFIED: FAIL" || echo "  Qualified: OK"
  grep -E 'reference/[a-z-]+/[a-z-]+\.md' "$f" && echo "  NESTED: FAIL" || echo "  Refs: OK"
  grep -c "## Worked Example" "$f"
done
```
  </action>
  <acceptance_criteria>
    - "Alle Checks PASS für beide Files"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sample-Live-Validation gegen Teamwork (Read-Only)</name>
  <action>
2 Read-Calls pro Recipe gegen port 19724:

**curtain-walls.md:**
- `elements_get_elements_by_type` mit `CurtainWall` → Anzahl > 0 erwartet.
- `elements_get_connected_elements` mit einem CurtainWall-GUID + `connectedElementType: "CurtainWallPanel"` → Sub-Elemente sichtbar.

**library-objects.md:**
- `elements_get_elements_by_type` mit `Object` → diverse Objects erwartet.
- Property-Read für 1 Object → library-part-name als Subtype-Identifier.
  </action>
  <acceptance_criteria>
    - "4 Read-Calls erfolgreich gegen Teamwork (Port 19724)"
    - "Strikt Read-Only — keine Schreib-Operation"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: SKILL.md Hinweis-Text feinjustieren (optional)</name>
  <action>
SKILL.md Recipe-Index zeigt bereits beide Recipes. Falls die aktuellen Hinweise zu kurz sind, leicht ergänzen (z. B. „Fassaden mit Sub-Element-Hierarchie" / „Möbel/Sanitär/Beleuchtung mit Property-Bulk-Pattern").
  </action>
  <acceptance_criteria>
    - "Hinweis-Texte präzise und nicht missverständlich"
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>2 neue Recipes (curtain-walls.md + library-objects.md), Konsistenz + Live-Sample-Validation.</what-built>
  <how-to-verify>Skim der 2 Recipes; ggf. fresh-session-Prompt „Liste mir alle Curtain Walls im Futurelab-Projekt".</how-to-verify>
  <resume-signal>"approved" oder konkrete Issues.</resume-signal>
</task>

</tasks>

<success_criteria>
- Phase 4 ist vollständig: 2 produktive Recipes, Skill konsistent, Live-nachweisbar.
</success_criteria>
