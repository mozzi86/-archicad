---
mode: quick
plan: 260906-jw7
type: execute
files_modified:
  - recipes/library-objects.md
  - recipes/oeffnungen-aus-konturen.md
  - recipes/zones.md
  - reference/mcp-conventions.md
autonomous: true
---

<objective>
Vier live verifizierte Erkenntnisse der THN-Sitzung 2026-09-06 (Nachmittag) in den
Archicad-Skill eintragen, jeweils mit `<!-- 2026-09-06 -->`, kurz, ohne Doppelung.
Vormittags-Erkenntnisse (Commit c6100f7) sind schon drin — nicht anfassen.
</objective>

<context>
@recipes/library-objects.md
@recipes/oeffnungen-aus-konturen.md
@recipes/zones.md
@reference/mcp-conventions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rotation-Zeile in library-objects.md korrigieren</name>
  <files>recipes/library-objects.md</files>
  <action>
  Zeile 767 ("Rotation: `angle` ist bei GDL-Objekten ein normaler GDL-Parameter →
  `SetGDLParametersOfElements`, nicht SetDetails.") ist falsch für die
  Durchbruch-Symbole und muss ERSETZT werden (nicht nur ergänzt) durch:
  Der GDL-Parameter `angle` ist ein interner (Text-)Winkel — Sollwert 180° wurde
  zu 3,1°, `details.angle` blieb 0 (sofort zurücksetzen bei Fehlversuch). Richtig
  ist Tapir `RotateElements`: `origin` = Objektmitte, berechnet aus Einfügepunkt
  + (A/2, B/2) im aktuellen Winkel gedreht (`cx = ox + (A/2)·cos a − (B/2)·sin a`,
  `cy = oy + (A/2)·sin a + (B/2)·cos a`), `beginPoint = (cx+1, cy)`,
  `endPoint = (cx+cos Δ, cy+sin Δ)`. Live 2026-09-06: 0°→270° bei 0,0 mm Versatz
  der Mitte, 141 Objekte. `SetDetailsOfElements` kennt kein `angle` (Schemafehler).
  Ergänzend festhalten: Die Tiefe `B` ist per `SetGDLParametersOfElements` setzbar
  und wirkt sowohl im GDL-Parameter als auch in `details.dimensions`.
  Marker `<!-- 2026-09-06 -->` an die geänderte Zeile/den Absatz anhängen.
  Bestehenden Stil (kurze Bulletpunkte) beibehalten, keine neue Überschrift.
  </action>
  <verify>
    <automated>grep -c "RotateElements" recipes/library-objects.md; grep -c "2026-09-06" recipes/library-objects.md</automated>
  </verify>
  <done>Die alte falsche Zeile existiert nicht mehr im Text; RotateElements-Rezept mit Datumsmarker steht an ihrer Stelle.</done>
</task>

<task type="auto">
  <name>Task 2: Host-Deckung messen — Punkt 3 präzisieren + neuen Abschnitt Drehen/Tiefe/Zentrieren ergänzen</name>
  <files>recipes/oeffnungen-aus-konturen.md</files>
  <action>
  Im Abschnitt "Host-Deckung messen, bevor man umbaut" (ab Zeile 103), Punkt 3
  ("Objektmitte = origin + (A/2, B/2)...") ERGÄNZEN mit dem live-verifizierten
  Zusatz: Vergleichsgröße ist immer die geometrische Mitte, nie `origin` — ein
  Lauf mit `origin` statt Mitte lieferte scheinbar schwankende Trefferzahlen
  (287 → 173 → 88); die Messung war falsch, nicht das Modell.
  Danach als neuen Unterabschnitt (mit `<!-- 2026-09-06 -->`) anfügen, nicht den
  heute vormittags eingefügten Geschosslogik/Beschriftungs-Abschnitt duplizieren:
  - Kriterium "mittig in der Wand": `|clearance + Dicke/2| ≤ 2 cm` (NICHT
    `|clearance|` — ein Objekt exakt auf der Achse hat `clearance = −Dicke/2`).
  - Reihenfolge Drehen → Tiefe → Zentrieren: MoveElements senkrecht zur
    Wandachse (längs bleibt Planposition); die kleinere Tiefe verschiebt die
    Mitte um (B_alt − B_neu)/2 — nach jedem Schritt neu messen und iterieren
    bis nichts mehr offen ist (THN Runde 2: 0/0/0).
  - Tiefe = Wanddicke gilt nur für Durchbrüche (WD/MD/HD/BSK); Schlitze
    (WS/SWS) gehen planmäßig nicht durch die Wand und behalten ihre Tiefe.
  - An Wandkreuzungen wird die Zuordnung nach dem Zentrieren mehrdeutig —
    bewusst belassen.
  </action>
  <verify>
    <automated>grep -c "2026-09-06" recipes/oeffnungen-aus-konturen.md; grep -c "Wandkreuzungen" recipes/oeffnungen-aus-konturen.md</automated>
  </verify>
  <done>Punkt 3 erweitert, neuer Unterabschnitt mit den vier Punkten vorhanden, kein Duplikat des vormittags-Geschosslogik-Abschnitts.</done>
</task>

<task type="auto">
  <name>Task 3: Haus/Bauteil ohne Zonen — passende Datei wählen und ergänzen</name>
  <files>recipes/zones.md</files>
  <action>
  recipes/zones.md ist thematisch passend (Zone-Membership / Bulk-Klassifizierung
  ist dort bereits behandelt; referenzmodell-abgleich.md behandelt ein anderes
  Thema — Versatz zwischen zwei Modellen — und passt nicht). Am Ende von
  recipes/zones.md, vor "Verwandte Recipes" oder als eigener Abschnitt
  "Gotchas" ergänzen, mit `<!-- 2026-09-06 -->`:
  Liegen im Fundbereich keine Zonen, liefert die Wand-Property "Bauteilname"
  (Gruppe "Allgemeine Werte", GUID projektspezifisch über `API.GetAllPropertyIds`
  + `GetDetailsOfProperties` suchen, per `GetPropertyValuesOfElements` an den
  Wänden im Bereich lesen) das Haus zuverlässig — THN Nr28: 83 von 93 Wänden "WA".
  Ordnername, Dateiname und Rühl-Stempel sind unzuverlässig: Ordner WA/Dateiname
  "BT-B" mit Plankopf "BT. C" = Haus WB (Nr24/27/32); Dateiname "BT-A" mit
  Plankopf "BAUTEIL D" = Haus WA (Nr28). Regel: Haus immer aus Zonen ODER
  Wand-Bauteilname ableiten, nie aus Datei-/Ordnernamen.
  </action>
  <verify>
    <automated>grep -c "Bauteilname" recipes/zones.md; grep -c "2026-09-06" recipes/zones.md</automated>
  </verify>
  <done>Abschnitt zu Bauteilname-Fallback ohne Zonen ist in zones.md vorhanden, mit Datumsmarker.</done>
</task>

<task type="auto">
  <name>Task 4: Betriebs-Fallen in mcp-conventions.md anhängen</name>
  <files>reference/mcp-conventions.md</files>
  <action>
  An den bestehenden Abschnitt "## Betriebs-Fallen (THN-Live-Sitzung) <!-- 2026-09-06 -->"
  (Zeile ~329) drei weitere Bulletpunkte ANHÄNGEN (keinen neuen Abschnitt
  erstellen):
  - Python puffert `print` in Pipes — `ps`-Laufzeit und Änderungszeit der
    Logdatei trennen "arbeitet" von "hängt", bevor man einen Lauf für hängend
    hält. Abhilfe: `sys.stdout.reconfigure(line_buffering=True)` oder direkt in
    die Datei schreiben; kein `| grep` hinter langen Läufen (grep puffert
    zusätzlich).
  - Modell-Durchgang über alle Objekte einmal je Lauf cachen statt je Geschoss
    wiederholen (2045 Objekte ≈ 8 Minuten pro Durchgang).
  - Parallele Lesezugriffe während eines Schreiblaufs erzeugen Zeitüberschreitungen
    — nur ein Prozess am Modell.
  </action>
  <verify>
    <automated>grep -c "line_buffering\|Zeitüberschreitungen" reference/mcp-conventions.md</automated>
  </verify>
  <done>Drei neue Bulletpunkte am Ende des bestehenden Betriebs-Fallen-Abschnitts, kein neuer Abschnitt erstellt.</done>
</task>

</tasks>

<success_criteria>
Alle vier Dateien enthalten die neuen, live verifizierten Erkenntnisse mit
`<!-- 2026-09-06 -->`-Markern, ohne Duplikate zu bestehendem Text oder zu den
Vormittags-Einträgen aus Commit c6100f7. Nichts wurde committet.
</success_criteria>
