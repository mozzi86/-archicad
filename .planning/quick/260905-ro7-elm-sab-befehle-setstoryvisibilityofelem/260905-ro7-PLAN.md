---
phase: quick-260905-ro7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ELM_SAB_Tapir/Sources/StoryVisibilityCommands.hpp
  - ELM_SAB_Tapir/Sources/StoryVisibilityCommands.cpp
  - ELM_SAB_Tapir/Sources/AddOnMain.cpp
  - ELM_SAB_Tapir/Sources/MigrationHelper.hpp
  - ELM_SAB_Tapir/README.md
  - reference/mcp-extension.md
autonomous: true
requirements: [QUICK-RO7]

must_haves:
  truths:
    - "ELM_SAB.SetStoryVisibilityOfElements setzt 'Auf Geschossen zeigen' an Objekten und Lampen und meldet nur dann Erfolg, wenn die Rücklese den Sollwert bestätigt."
    - "ELM_SAB.GetStoryVisibilityOfElements liefert je Element Preset-Name plus die fünf Rohfelder und das Ursprungsgeschoss."
    - "Beide Befehle sind im ELM_SAB-Block registriert und werden vom CI-Build für AC27/28/29 mitgebaut (CMakeLists bleibt unberührt, GLOB)."
    - "README-Befehlstabelle und reference/mcp-extension.md nennen die beiden neuen Befehle."
  artifacts:
    - path: "ELM_SAB_Tapir/Sources/StoryVisibilityCommands.hpp"
      provides: "Klassendeklarationen SetStoryVisibilityOfElementsCommand + GetStoryVisibilityOfElementsCommand"
    - path: "ELM_SAB_Tapir/Sources/StoryVisibilityCommands.cpp"
      provides: "Schemas, Preset-Mapping, ApplyToOne mit Rücklese, Execute für beide Befehle"
    - path: "ELM_SAB_Tapir/Sources/AddOnMain.cpp"
      provides: "Include + zwei RegisterCommand-Zeilen im elmSabCommands-Block"
  key_links:
    - from: "ELM_SAB_Tapir/Sources/AddOnMain.cpp"
      to: "StoryVisibilityCommands.hpp"
      via: "#include + RegisterCommand<...> (elmSabCommands, \"0.9.15\", ...)"
      pattern: "RegisterCommand<(Set|Get)StoryVisibilityOfElementsCommand>"
    - from: "ELM_SAB_Tapir/Sources/StoryVisibilityCommands.cpp"
      to: "API_ObjectType.visibility / isAutoOnStoryVisibility"
      via: "ACAPI_Element_Change mit Maske + zweitem ACAPI_Element_Get"
      pattern: "ACAPI_ELEMENT_MASK_SET \\(mask, API_ObjectType,"
---

<objective>
Zwei neue ELM_SAB-Befehle ins Kombi-Add-On `ELM_SAB_Tapir` einbauen:
`SetStoryVisibilityOfElements` und `GetStoryVisibilityOfElements` — die
Elementeinstellung „Auf Geschossen zeigen" von Objekten und Lampen lesen und setzen.

Purpose: Weder Tapir noch die offizielle JSON-API fassen `API_ObjectType::visibility`
oder `isAutoOnStoryVisibility` an (belegt: null Treffer im Repo-Grep). Ohne diese
Befehle bleibt für ~3.200 Objekte am THN nur manuelles Klicken.

Output: 2 neue Quelldateien, 3 Zeilen `AddOnMain.cpp`, 1 Zeile `MigrationHelper.hpp`,
2 Doku-Ergänzungen. Kein lokaler Build — der CI-Workflow `build-elm-sab-tapir.yml`
baut beim Push (Matrix AC27/28/29 × Mac/Win).
</objective>

<context>
@/Users/ap/.scratch/thn-handoff/sud_kampagne/elm_sab_storyvisibility_design.md
@ELM_SAB_Tapir/Sources/SetColumnRotationCommand.cpp
@ELM_SAB_Tapir/Sources/ELMCommandBase.hpp
@ELM_SAB_Tapir/Sources/CommandBase.hpp

**Der Entwurf ist die Vorgabe.** §3 des Design-Dokuments enthält den vollständigen
Code für beide Dateien plus die Registrierungszeilen. Umsetzen, nicht neu erfinden.
Abweichungen nur dort, wo dieser Plan sie unten ausdrücklich nennt.
</context>

<interfaces>
<!-- Aus dem Repo extrahiert. Der Executor braucht dafür keine eigene Suche. -->

Basisklasse — `ELM_SAB_Tapir/Sources/ELMCommandBase.hpp:14–54`:
`class ELMCommandBase : public API_AddOnCommand` liefert `GetNamespace() == "ELM_SAB"`
(final) und `ScheduleForExecutionOnMainThread` (final). Ableitungen überschreiben nur
`GetName`, `GetInputParametersSchema`, `GetResponseSchema`, `Execute`.
Header inkludiert bereits `APIEnvir.h`, `ACAPinc.h`, `ObjectState.hpp`, `CommandBase.hpp`.

Helfer aus `CommandBase.hpp` (transitiv über `ELMCommandBase.hpp` verfügbar):
- `:38–40` `CreateErrorResponse (GSErrCode, const GS::UniString&)`,
  `CreateFailedExecutionResult (GSErrCode, const GS::UniString&)`,
  `CreateSuccessfulExecutionResult ()`
- `:42` `API_Guid GetGuidFromObjectState (const GS::ObjectState&)`
- `:44` `inline API_Guid GetGuidFromElementsArrayItem (const GS::ObjectState&)`
- `:60` `inline GS::ObjectState CreateGuidObjectState (const API_Guid&)`
- `:8` zieht `BiHashTable.hpp` — `GS::HashTable<API_Guid, short>` ist damit ohne
  Zusatz-Include nutzbar (so macht es `TeamworkCommands.cpp:139` auch).

Teamwork-Signaturen, wie im Repo verwendet:
- `TeamworkCommands.cpp:141` `ACAPI_Teamwork_ReserveElements (const GS::Array<API_Guid>& elemIds, GS::HashTable<API_Guid, short>* conflicts, bool enableDialogs)`
- `TeamworkCommands.cpp:220` `ACAPI_Teamwork_ReleaseElements (const GS::Array<API_Guid>& elemIds, bool enableDialogs)`
- DevKit AC29 `ACAPinc.h:7522` `__APIEXPORT bool ACAPI_Teamwork_HasConnection (void);`

ObjectState-Zugriffsmuster (belegt in `SetColumnRotationCommand.cpp:54–56`):
- Zeiger-Form: `const GS::ObjectState* p = os.Get ("feld");` → `nullptr` wenn nicht da
- Bool-Form: `bool ok = os.Get ("feld", zielVariable);`
- Listen: `const auto& liste = response.AddList<GS::ObjectState> ("name"); liste (item);`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Neue Command-Dateien aus §3 des Entwurfs anlegen</name>
  <files>ELM_SAB_Tapir/Sources/StoryVisibilityCommands.hpp, ELM_SAB_Tapir/Sources/StoryVisibilityCommands.cpp</files>
  <action>
Beide Dateien exakt nach §3 des Design-Dokuments anlegen (Header ~20 Zeilen, .cpp ~310 Zeilen),
mit folgenden Präzisierungen, die aus dem Repo-Abgleich stammen und im Entwurf noch offen sind:

1. **`#include "MigrationHelper.hpp"` direkt nach `#include "StoryVisibilityCommands.hpp"`
   in der .cpp.** Ohne diesen Include existieren die Namen `ACAPI_Teamwork_*` in den
   Vor-AC27-Zweigen nicht; `TeamworkCommands.cpp:2` macht es genauso. Der Entwurf
   erwähnt den Include nicht — er ist Pflicht.

2. **Union-Zugriff für Lampen über `element.object`.** `API_LampType` ist nur ein
   `using`-Alias auf `API_ObjectType` (DevKit `APIdefs_Elements.h:5713`), beide Union-Member
   liegen am selben Offset. Also für `API_ObjectID` UND `API_LampID` durchgehend
   `element.object.…` schreiben — kein `if (typ == Lamp) element.lamp` Zweig. Ebenso in
   der Maske durchgehend `API_ObjectType` als Typ-Argument.

3. **Kommentarkopf** analog `SetColumnRotationCommand.hpp:1–6`: was der Befehl tut, warum
   er existiert (Tapir/JSON-API können diese Felder nicht), Datum 2026-09-05, und der
   Hinweis aus DevKit `APIdefs_Elements.h:950–955`, dass `showRelAbove/showRelBelow` für
   `API_ObjectType` laut Header-Doku „not extended" ist — deshalb die Rücklese.

4. **C++17, kein C++20.** Keine designierten Initialisierer, kein `<ranges>`, kein
   `std::span`. `std::vector` ist erlaubt (`#include <vector>`), so wie im Entwurf.

5. **Rücklese ist der Erfolgsmaßstab.** `ApplyToOne` darf nach `ACAPI_Element_Change`
   niemals `NoError` zurückgeben, ohne per zweitem `ACAPI_Element_Get` alle sechs Felder
   gegen den Soll-`VisSpec` verglichen zu haben — bei `isAuto` nur
   `isAutoOnStoryVisibility`, weil Archicad die Visibility-Struct dann selbst neu berechnet.

6. **Alle sechs Maskenfelder setzen** (`isAutoOnStoryVisibility`, `visibility.showOnHome`,
   `.showAllAbove`, `.showAllBelow`, `.showRelAbove`, `.showRelBelow`), gepunktete
   Schreibweise wie DevKit-Konvention und `ExtendedElementCommands.cpp:221`.

7. **Preset-Vokabular** wie im Entwurf, sechs Werte: `HomeOnly`, `HomeAndOneUp`,
   `HomeAndOneDown`, `HomeAndOneUpAndDown`, `AllStories`, `AllRelevant`. Unbekannter
   String ⇒ `CreateErrorResponse (APIERR_BADPARS, …)`. `SpecToPreset` gibt `"Custom"`
   zurück, wenn keine Kombination passt.

8. **Fehlertexte ASCII-transliteriert** („Ruecklese", „Aenderung") — der bestehende
   ELM_SAB-Block hält es so.
  </action>
  <verify>
    <automated>test -f ELM_SAB_Tapir/Sources/StoryVisibilityCommands.hpp && test -f ELM_SAB_Tapir/Sources/StoryVisibilityCommands.cpp && awk '{o+=gsub(/\{/,"&"); c+=gsub(/\}/,"&")} END{if(o!=c){print "UNBALANCED braces: "o" vs "c; exit 1}; print "braces ok: "o}' ELM_SAB_Tapir/Sources/StoryVisibilityCommands.cpp && test "$(grep -c 'ACAPI_ELEMENT_MASK_SET (mask, API_ObjectType,' ELM_SAB_Tapir/Sources/StoryVisibilityCommands.cpp)" = "6" && grep -q '#include "MigrationHelper.hpp"' ELM_SAB_Tapir/Sources/StoryVisibilityCommands.cpp && test "$(grep -c 'ACAPI_Element_Get' ELM_SAB_Tapir/Sources/StoryVisibilityCommands.cpp)" -ge 3 && ! grep -qn 'element\.lamp\|check\.lamp' ELM_SAB_Tapir/Sources/StoryVisibilityCommands.cpp && echo "TASK1 OK"</automated>
  </verify>
  <done>
Beide Dateien existieren. Geschweifte Klammern balanciert (die JSON-Rohstrings sind in
sich balanciert, deshalb ist der Zähler aussagekräftig). Genau 6 Maskenzeilen mit
`API_ObjectType`. `MigrationHelper.hpp` inkludiert. Mindestens 3 `ACAPI_Element_Get`
(Set: Lesen + Rücklese, Get: Lesen). Kein `.lamp`-Union-Zugriff.
  </done>
</task>

<task type="auto">
  <name>Task 2: Registrierung in AddOnMain.cpp + MigrationHelper-Zeile</name>
  <files>ELM_SAB_Tapir/Sources/AddOnMain.cpp, ELM_SAB_Tapir/Sources/MigrationHelper.hpp</files>
  <action>
**AddOnMain.cpp, Include:** `#include "StoryVisibilityCommands.hpp"` in den ELM_SAB-Include-Block
einfügen — dieser Block beginnt mit dem Kommentar `// ELM_SAB-Befehle (Namespace "ELM_SAB")`
und endet mit `#include "GetVersionCommandELM.hpp"`. Neue Zeile hinter
`#include "CaptureViewCommand.hpp"` setzen.

**AddOnMain.cpp, Registrierung:** Im Block `{ // ELM_SAB Commands (Namespace "ELM_SAB") }`
unmittelbar VOR `AddCommandGroup (elmSabCommands);` zwei Registrierungen anfügen, im Stil
der Nachbarn (`err |= RegisterCommand<…> (elmSabCommands, "0.9.15", "ELM_SAB: …");`):

- `SetStoryVisibilityOfElementsCommand`, Version `"0.9.15"`, Beschreibung sinngemäß:
  „ELM_SAB: Setzt 'Auf Geschossen zeigen' von Objekten und Lampen (Preset oder custom) —
  weder Tapir noch die JSON-API koennen das; mit Ruecklese-Verifikation und optionaler
  Teamwork-Reservierung."
- `GetStoryVisibilityOfElementsCommand`, Version `"0.9.15"`, Beschreibung sinngemäß:
  „ELM_SAB: Liest 'Auf Geschossen zeigen' von Objekten und Lampen (Preset plus Rohfelder
  plus Ursprungsgeschoss)."

Beschreibungstexte ASCII-transliteriert (keine Umlaute), wie im ganzen Block üblich.
Doppelte Anführungszeichen im Text vermeiden — einfache Anführungszeichen nutzen, statt
`\"` zu escapen.

**CMakeLists.txt NICHT anfassen.** `Tools/CMakeCommon.cmake:160` sammelt `Sources/*.cpp`
per `GLOB_RECURSE CONFIGURE_DEPENDS`; neue Dateien werden automatisch mitgebaut.

**MigrationHelper.hpp:** Eine Zeile
`#define ACAPI_Teamwork_HasConnection ACAPI_TeamworkControl_HasConnection`
zu den anderen `ACAPI_Teamwork_*`-Defines (aktuell Zeilen 35–39) im Block
`#ifndef ServerMainVers_2700` hinzufügen. Einordnung: Dieser Block gilt für AC26 und
älter, die CI-Matrix baut AC27/28/29 — die Zeile ist für die CI also inert und dient nur
der Konsistenz mit den vier Nachbar-Defines. Falls die CI wider Erwarten meldet, dass
`ACAPI_Teamwork_HasConnection` in AC27/28 fehlt, ist der Rückfallweg aus §3 des Entwurfs
zu nehmen: `HasConnection`-Abfrage weglassen und `ACAPI_Teamwork_ReserveElements`
unbedingt aufrufen (im Solo-Projekt schlägt es folgenlos fehl).
  </action>
  <verify>
    <automated>bash -c 'set -e; A=ELM_SAB_Tapir/Sources/AddOnMain.cpp; grep -q "#include \"StoryVisibilityCommands.hpp\"" $A; test "$(grep -c "RegisterCommand<SetStoryVisibilityOfElementsCommand>\|RegisterCommand<GetStoryVisibilityOfElementsCommand>" $A)" = 2; S=$(awk "/\\/\\/ ELM_SAB Commands/{print NR; exit}" $A); E=$(awk "/AddCommandGroup \\(elmSabCommands\\)/{print NR; exit}" $A); N=$(awk -v s=$S -v e=$E "NR>s && NR<e && /StoryVisibilityOfElementsCommand/{n++} END{print n+0}" $A); test "$N" = 2 || { echo "nicht im elmSabCommands-Block: $N"; exit 1; }; grep -q "ACAPI_Teamwork_HasConnection ACAPI_TeamworkControl_HasConnection" ELM_SAB_Tapir/Sources/MigrationHelper.hpp; if git diff --name-only | grep -q "CMakeLists.txt"; then echo "FEHLER: CMakeLists angefasst"; exit 1; fi; echo "TASK2 OK"' </automated>
  </verify>
  <done>
Include vorhanden, genau 2 RegisterCommand-Zeilen, beide nachweislich zwischen
`// ELM_SAB Commands` und `AddCommandGroup (elmSabCommands)`. MigrationHelper-Define
vorhanden. `CMakeLists.txt` unverändert.
  </done>
</task>

<task type="auto">
  <name>Task 3: Doku — README-Befehlstabelle und Skill-Referenz</name>
  <files>ELM_SAB_Tapir/README.md, reference/mcp-extension.md</files>
  <action>
**`ELM_SAB_Tapir/README.md`:** In der Befehlstabelle (Kopf `| Befehl | Wofür |` bei Zeile 79)
nach der letzten Zeile (`SetColumnRotation`, Zeile 89) eine Zeile anfügen:

`| GetStoryVisibilityOfElements / SetStoryVisibilityOfElements | „Auf Geschossen zeigen" von Objekten und Lampen lesen und setzen. Presets (HomeOnly … AllRelevant) oder Rohfelder. Weder Tapir noch die offizielle JSON-API fassen diese Felder an. |`

Befehlsnamen in Backticks setzen, wie die Nachbarzeilen. Deutsche Umlaute sind in der
README erlaubt (anders als in den C++-Beschreibungsstrings).

**`reference/mcp-extension.md`:** Am Dateiende (nach Zeile 865) einen neuen datierten
Abschnitt anhängen, im Stil der bestehenden Log-Einträge:
`## ELM_SAB 0.9.15 — Auf Geschossen zeigen <!-- 2026-09-05 -->`

Inhalt, knapp (10–20 Zeilen):
- Was neu ist: `ELM_SAB.SetStoryVisibilityOfElements` / `.GetStoryVisibilityOfElements`,
  nur Object und Lamp (`API_LampType` ist ein Alias auf `API_ObjectType`).
- Parameter Set: `elements[]`, entweder `visibility` (Enum HomeOnly / HomeAndOneUp /
  HomeAndOneDown / HomeAndOneUpAndDown / AllStories / AllRelevant) oder `custom`
  ({showOnHome, showAllAbove, showAllBelow, showRelAbove 0|1, showRelBelow 0|1}),
  plus `reserve` (Default true). Antwort: `executionResults[]`, bei Erfolg mit
  zurückgelesenem `visibility`.
- Antwort Get: `storyVisibilities[]` je Element mit `visibility`,
  `isAutoOnStoryVisibility`, den vier Rohfeldern und `homeStory`.
- **Warum es diesen Befehl braucht:** Tapirs `SetDetailsOfElements` kann nur `floorInd`
  (= Ursprungsgeschoss), nicht die Geschoss-Sichtbarkeit; die offizielle JSON-API kennt
  kein Property dafür.
- **Offene Verifikation:** Die DevKit-Header-Doku sagt, das Relativ-Geschoss-Feature sei
  für `API_ObjectType` „not extended" — `showRelAbove/showRelBelow` sind an Objekten
  unerprobt. Vor jedem Massenlauf an EINEM Testobjekt setzen, per
  `GetStoryVisibilityOfElements` und in der Objekt-UI gegenlesen. Fallback:
  `AllRelevant` (per DevKit-Beispiel belegt) oder `AllStories`.
- Der Abschnitt beschreibt einen noch nicht live verifizierten Befehl. Das MUSS dort
  stehen — als „ungetestet, Build läuft in CI", nicht als bestätigte Fähigkeit. Erst nach
  Live-Test am Modell darf der Vermerk auf verifiziert geändert werden.
  </action>
  <verify>
    <automated>grep -q 'SetStoryVisibilityOfElements' ELM_SAB_Tapir/README.md && awk '/^\| Befehl \| Wof/{t=1} t&&/StoryVisibility/{n++} END{if(n!=1){print "README-Tabellenzeile fehlt/doppelt: "n+0; exit 1}; print "readme ok"}' ELM_SAB_Tapir/README.md && grep -q '^## ELM_SAB 0.9.15' reference/mcp-extension.md && grep -q '2026-09-05' reference/mcp-extension.md && grep -qi 'ungetestet\|nicht live verifiziert\|unerprobt' reference/mcp-extension.md && echo "TASK3 OK"</automated>
  </verify>
  <done>
README-Tabelle enthält genau eine neue Zeile für die Story-Visibility-Befehle.
`reference/mcp-extension.md` hat den datierten Abschnitt `## ELM_SAB 0.9.15` mit
Datumsmarker 2026-09-05 und einem ausdrücklichen „noch nicht live verifiziert"-Vermerk.
  </done>
</task>

</tasks>

<verification>
Gesamtprüfung nach allen drei Tasks:

1. Nur die sechs geplanten Dateien sind angefasst:
   `git status --porcelain` zeigt ausschließlich die zwei neuen `StoryVisibilityCommands.*`
   sowie geänderte `AddOnMain.cpp`, `MigrationHelper.hpp`, `README.md`, `mcp-extension.md`.
   Insbesondere KEIN `CMakeLists.txt`.
2. Kein C++20: `grep -n 'std::span\|<ranges>\|co_await\|consteval' ELM_SAB_Tapir/Sources/StoryVisibilityCommands.cpp` liefert nichts.
3. Sichtprüfung der .cpp gegen §3 des Entwurfs: `PresetToSpec` deckt alle sechs Presets ab,
   `SpecToPreset` ist dazu invers, `ApplyToOne` liest nach jedem `ACAPI_Element_Change` zurück.
4. **Kein lokaler Build.** Der CI-Workflow `build-elm-sab-tapir.yml` baut beim Push
   (Matrix AC27/28/29 × Mac/Win, `fail-fast: false`). Ein grüner AC27-Job ist der einzige
   belastbare Beweis für Task 2; ein roter AC27-Job löst den Rückfallweg aus Task 2 aus.
5. **Nichts committen, nichts pushen** — das übernimmt der Executor nach Freigabe.
</verification>

<success_criteria>
- `ELM_SAB_Tapir/Sources/StoryVisibilityCommands.hpp/.cpp` existieren und setzen §3 des
  Entwurfs um, inklusive der sechs Maskenfelder und der Rücklese-Verifikation.
- Beide Befehle sind mit Version `0.9.15` im `elmSabCommands`-Block registriert.
- `MigrationHelper.hpp` enthält das `ACAPI_Teamwork_HasConnection`-Define.
- README-Befehlstabelle und `reference/mcp-extension.md` nennen die Befehle, letztere mit
  ausdrücklichem „noch nicht live verifiziert"-Vermerk.
- `CMakeLists.txt` unverändert; keine C++20-Konstrukte; kein lokaler Build ausgeführt.
</success_criteria>

<output>
Nach Abschluss `.planning/quick/260905-ro7-elm-sab-befehle-setstoryvisibilityofelem/260905-ro7-SUMMARY.md` schreiben.
</output>
