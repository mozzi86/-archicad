---
phase: quick-260909-0tg
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recipes/library-objects.md
  - recipes/oeffnungen-aus-konturen.md
  - reference/mcp-conventions.md
autonomous: true
requirements: [QUICK-260909-0tg]
must_haves:
  truths:
    - "Wer library-objects.md liest, weiß vor einem Massenlauf, dass A/B/ZZYZX bei den SAB-Durchbruch-Symbolen abgeleitete Werte sind und gs_hole_* / gs_slab_thickness die Steuerparameter."
    - "Wer oeffnungen-aus-konturen.md liest, kennt die THN-Zahlen (2110 betroffen, 1856/1856 repariert) und die Abnahmeregel Maße-gegen-Register."
    - "Wer mcp-conventions.md liest, weiß, dass leeres executionResults sitzungsweit auftritt, Neustart hilft und SetObjectParametersForce undoScope meldet."
    - "Alle drei Einträge tragen den Marker <!-- 2026-09-09 --> und wiederholen den 0.9.16-Abschnitt in mcp-extension.md nicht."
  artifacts:
    - path: "recipes/library-objects.md"
      provides: "Abschnitt Steuerparameter vs. abgeleitete Werte"
      contains: "gs_hole_width"
    - path: "recipes/oeffnungen-aus-konturen.md"
      provides: "Massenlauf-/Abnahmeregel für Durchbruch-Symbole"
      contains: "gs_slab_thickness"
    - path: "reference/mcp-conventions.md"
      provides: "Nachtrag sitzungsweites leeres executionResults"
      contains: "undoScope"
  key_links:
    - from: "recipes/oeffnungen-aus-konturen.md"
      to: "recipes/library-objects.md"
      via: "Querverweis statt Duplikat"
      pattern: "library-objects.md"
    - from: "reference/mcp-conventions.md"
      to: "reference/mcp-extension.md"
      via: "Verweis auf den 0.9.16-Abschnitt"
      pattern: "mcp-extension.md"
---

<objective>
Zwei Erkenntnisse vom 2026-09-09 in den Skill eintragen: (1) GDL-Steuerparameter
der SAB-Durchbruch-Symbole (A/B/ZZYZX sind abgeleitet), (2) Nachtrag zum
Diagnosemuster „leeres executionResults" als Sitzungszustand.

Purpose: Verhindert den nächsten Massenlauf, der stillschweigend auf
Bibliotheks-Defaults zurückfällt (THN: 2110 Objekte), und macht die
Neustart-Abhilfe auffindbar.
Output: Drei ergänzte Markdown-Dateien, ein atomarer Commit, push origin main.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@recipes/library-objects.md
@recipes/oeffnungen-aus-konturen.md
@reference/mcp-conventions.md

Bereits vorhanden — NICHT wiederholen, nur verlinken:
- reference/mcp-extension.md, Abschnitt „Leere `executionResults[]` = Undo-Scope hat
  nicht gestartet" (<!-- 2026-09-08 -->, ab Zeile 894) und „ELM_SAB 0.9.16 —
  `SetObjectParametersForce`" (<!-- 2026-09-08 -->, ab Zeile 930).
- recipes/library-objects.md Zeile 762 (`dimensions` mappt auf A/B/ZZYZX) und
  Gotcha 9 (Zeile 703, `dimensions`-Override-Caveat) — der neue Eintrag ist die
  konkrete Ausprägung davon und muss darauf verweisen statt sie zu wiederholen.
- recipes/oeffnungen-aus-konturen.md Zeile 97 (B/ZZYZX-Missbrauch) und die
  Abschnitte mit <!-- 2026-09-06 --> / <!-- 2026-09-07 -->.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Steuerparameter-Abschnitt in library-objects.md</name>
  <files>recipes/library-objects.md</files>
  <action>
Neuen Abschnitt am Ende der Datei anhängen (nach der Rotation-Zeile im Abschnitt
„Objekte per API erstellen und vollständig ausstatten"), Überschrift auf Ebene 2
mit Marker `<!-- 2026-09-09 -->`, Titel sinngemäß „Steuerparameter vs. abgeleitete
Werte — vor jedem Massenlauf klären".

Inhalt, in der Prosa-Form der übrigen Einträge (kein Bullet-Wust, deutsche
Rechtschreibung, Dezimalkomma):
- Bei den SAB-Bibliotheksobjekten „Deckendurchbruch Symbol" und „Bodendurchbruch
  Symbol" sind `A`, `B` und `ZZYZX` **abgeleitete** Werte: das Parameterskript
  rechnet sie aus den Steuerparametern und überschreibt jeden direkt gesetzten Wert.
- Steuerparameter benennen: `gs_hole_width` (Breite), `gs_hole_depth` (Tiefe),
  `gs_slab_thickness` (Höhe/Deckenstärke), `gs_hole_diameter` (runde Durchbrüche).
- Folge: `dimensions` beim `CreateObjects` und direktes Setzen von `A`/`B`/`ZZYZX`
  fallen auf die Bibliotheks-Defaults 0,70/0,40/0,30 zurück — der Aufruf meldet
  dabei Erfolg. Live THN 2026-09-09: 2110 Objekte betroffen, Reparatur 1856/1856
  über die Steuerparameter (Skript `mass_reparatur.py`).
- Regel: Vor einem Massenlauf an einem GDL-Objekt an EINEM Exemplar prüfen, welche
  Parameter Steuer- und welche abgeleitete Werte sind — setzen und über
  `GetDetailsOfElements` (`dimensions`) **und** `GetGDLParametersOfElements`
  gegenlesen; erst dann den Batch fahren.
- Querverweis auf Gotcha 9 (`dimensions`-Override-Caveat) als konkreten Live-Fall,
  ohne dessen Text zu wiederholen.

Keine Wiederholung des 0.9.16-/`SetObjectParametersForce`-Textes; höchstens ein
Verweis auf `../reference/mcp-extension.md`.
  </action>
  <verify>
    <automated>grep -v '^<!--' recipes/library-objects.md | grep -q "gs_hole_width" && grep -q "2026-09-09" recipes/library-objects.md && echo OK</automated>
  </verify>
  <done>Abschnitt existiert, nennt alle vier Steuerparameter, die Defaults 0,70/0,40/0,30, die Zahlen 2110 / 1856, `mass_reparatur.py` und den Marker <!-- 2026-09-09 -->.</done>
</task>

<task type="auto">
  <name>Task 2: Massenlauf-/Abnahmeregel in oeffnungen-aus-konturen.md + Conventions-Nachtrag</name>
  <files>recipes/oeffnungen-aus-konturen.md, reference/mcp-conventions.md</files>
  <action>
**a) recipes/oeffnungen-aus-konturen.md** — neuen Abschnitt am Ende anhängen,
Überschrift Ebene 2 mit `<!-- 2026-09-09 -->`, sinngemäß „Maße der
Durchbruch-Symbole: Steuerparameter setzen, Register gegenlesen".
Kurz halten (ca. 8–12 Zeilen), Fachkontext statt Parameterliste — die
Parameterliste steht in library-objects.md und wird von hier **verlinkt**
(`[../recipes/library-objects.md]`-Stil analog zu den bestehenden Querverweisen):
- Maße der Deckendurchbruch-/Bodendurchbruch-Symbole nie über `dimensions` oder
  `A`/`B`/`ZZYZX` erzeugen — nur über `gs_hole_width`/`gs_hole_depth`/
  `gs_slab_thickness` (rund: `gs_hole_diameter`).
- Live THN 2026-09-09: 2110 Objekte standen auf 0,70/0,40/0,30, 1856/1856
  repariert.
- Abnahmeregel: Die Abnahme eines Durchbruch-Laufs prüft die tatsächlichen Maße
  gegen das Register/die Fundliste — „Objekt existiert" und „Befehl meldete
  Erfolg" reichen nicht. Anschluss an die bestehende Nachlese-Regel
  <!-- 2026-09-07 -->.
- Verweis auf die Anti-Pattern-Zeile zu `B`/`ZZYZX` (Zeile ~97) statt Wiederholung.

**b) reference/mcp-conventions.md** — Nachtrag am Ende anhängen, Überschrift
Ebene 2 mit `<!-- 2026-09-09 -->`, sinngemäß „Nachtrag: leeres `executionResults`
ist sitzungsweit". Maximal ~10 Zeilen, weil die Analyse in
`mcp-extension.md` steht und dorthin verlinkt wird:
- Das Muster tritt nicht nur je Element, sondern **sitzungsweit** auf: startet
  `ACAPI_CallUndoableCommand` das Lambda nicht, wirft Tapir dessen Rückgabewert weg
  und meldet trotzdem Erfolg.
- Abhilfe, die live wirkt: **Archicad neu starten** — der Zustand ist klebrig und
  hält bis dahin.
- `ELM_SAB.SetObjectParametersForce` (0.9.16) macht den Zustand sichtbar:
  `undoScope.executed` / `errorCode` / `mode`. Details nur verlinken:
  `mcp-extension.md`, Abschnitte <!-- 2026-09-08 -->.
- Ein Satz Nachdokumentation: Der Add-on-Umbau 0.9.16 wurde ausnahmsweise auf
  Nutzer-Auftrag direkt committet und wird hier nachträglich dokumentiert.
  </action>
  <verify>
    <automated>grep -c "2026-09-09" recipes/oeffnungen-aus-konturen.md reference/mcp-conventions.md && grep -n "mcp-extension.md" reference/mcp-conventions.md</automated>
  </verify>
  <done>Beide Dateien haben je einen neuen 2026-09-09-Abschnitt; oeffnungen verlinkt library-objects.md, mcp-conventions verlinkt mcp-extension.md; kein Absatz aus dem 0.9.16-Abschnitt ist kopiert.</done>
</task>

<task type="auto">
  <name>Task 3: Dublettenprüfung, Commit, Push</name>
  <files>recipes/library-objects.md, recipes/oeffnungen-aus-konturen.md, reference/mcp-conventions.md</files>
  <action>
1. Dublettenprüfung: `git diff` der drei Dateien lesen und gegen die Abschnitte
   <!-- 2026-09-06 -->, <!-- 2026-09-07 --> sowie den 0.9.16-Abschnitt in
   `reference/mcp-extension.md` halten. Kein Satz darf inhaltlich doppelt stehen;
   Überschneidungen zu einem Querverweis kürzen.
2. Atomarer Commit der drei Dateien:
   `docs(quick-260909-0tg): GDL-Steuerparameter der Durchbruch-Symbole + sitzungsweites leeres executionResults`
   mit Trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
3. `git push origin main`.
  </action>
  <verify>
    <automated>git log -1 --format=%s%n%b | grep -q "quick-260909-0tg" && git status --porcelain recipes reference | wc -l | grep -q '^0$' && git log origin/main -1 --format=%s | grep -q "quick-260909-0tg"</automated>
  </verify>
  <done>Ein Commit mit den drei Dateien und dem Co-Authored-By-Trailer liegt auf origin/main; Arbeitsverzeichnis sauber.</done>
</task>

</tasks>

<verification>
- `grep -c "2026-09-09"` liefert in allen drei Dateien mindestens 1.
- `grep -n "gs_hole_width\|gs_hole_depth\|gs_slab_thickness\|gs_hole_diameter" recipes/library-objects.md` zeigt alle vier Steuerparameter.
- Kein Satz des 0.9.16-Abschnitts aus `reference/mcp-extension.md` erscheint wörtlich in `reference/mcp-conventions.md`.
- `git status --porcelain` ist leer, `git log origin/main -1` zeigt den Commit.
</verification>

<success_criteria>
Beide Erkenntnisse sind im Skill auffindbar, ohne Doppelung, mit Datumsmarker,
in einem atomaren Commit auf origin/main.
</success_criteria>

<output>
Create `.planning/quick/260909-0tg-gdl-steuerparameter-durchbruch-symbole-u/260909-0tg-SUMMARY.md` when done
</output>
