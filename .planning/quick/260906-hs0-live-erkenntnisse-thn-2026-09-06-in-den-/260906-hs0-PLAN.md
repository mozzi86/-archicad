---
phase: quick-260906-hs0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - reference/attribute-und-ebenen.md
  - recipes/oeffnungen-aus-konturen.md
  - reference/mcp-conventions.md
autonomous: true
requirements: [QUICK-HS0]

must_haves:
  truths:
    - "Wer layerIndex für SetDetailsOfElements auf einer leeren Ebene braucht, findet in attribute-und-ebenen.md den nativen Weg (API.GetAttributesIndices) statt Attributlisten-Position zu erraten."
    - "Wer die Geschossdarstellung + Beschriftung der Durchbruch-Symbole (BD/DD/Bodenschlitz) einrichtet, findet in oeffnungen-aus-konturen.md die Symbol-Eigenlogik und einen Verweis auf ELM_SAB.SetStoryVisibilityOfElements, ohne dass der Befehl dort erneut voll erklärt wird."
    - "Wer MCP-Zeitgrenzen, Aktionsbefehl-Risiken oder Archicad-Beenden falsch handhabt, findet in mcp-conventions.md die drei THN-2026-09-06-Betriebsregeln."
  artifacts:
    - path: "reference/attribute-und-ebenen.md"
      provides: "layerIndex-Gotcha (Index ≠ Listenposition) mit live THN-Werten"
      contains: "GetAttributesIndices"
    - path: "recipes/oeffnungen-aus-konturen.md"
      provides: "Geschosslogik + Beschriftungs-Grammatik der Durchbruch-Symbole"
      contains: "SetStoryVisibilityOfElements"
    - path: "reference/mcp-conventions.md"
      provides: "Betriebs-Fallen THN 2026-09-06 (Timeout, Aktionsbefehle, Beenden)"
      contains: "600 s"
  key_links:
    - from: "recipes/oeffnungen-aus-konturen.md"
      to: "reference/mcp-extension.md"
      via: "Markdown-Link auf den bestehenden Abschnitt 'ELM_SAB 0.9.15 — Auf Geschossen zeigen'"
      pattern: "mcp-extension\\.md#elm_sab-0915"
---

<objective>
Drei live am THN 2026-09-06 verifizierte Erkenntnisse in den Archicad-Skill eintragen —
jede an ihrem inhaltlich passenden Ort, mit Datums-Marker `<!-- 2026-09-06 -->`, ohne
Wiederholung von bereits Dokumentiertem (insbesondere nicht den ELM_SAB-Befehl aus
Commit 0803c3f erneut ausbuchstabieren).

Purpose: Der Skill wächst selbst-verbessernd pro Session (Projekt-Constraint). Diese
drei Punkte wurden heute an einem laufenden Teamwork-Projekt (THN) verifiziert und
verhindern beim nächsten Mal denselben Reibungsverlust (falscher Index geraten,
Symbol ohne Geschossdarstellung erstellt, Selektion durch Schema-Test-Aufruf verloren).

Output: Drei gezielte Textergänzungen in bestehenden Referenz-/Recipe-Dateien, keine
neuen Dateien, kein Code, kein Build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@reference/attribute-und-ebenen.md
@recipes/oeffnungen-aus-konturen.md
@reference/mcp-conventions.md
@reference/mcp-extension.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Layer-Index-Gotcha in attribute-und-ebenen.md ergänzen</name>
  <files>reference/attribute-und-ebenen.md</files>
  <action>
Im Abschnitt „## Ebenen" steht aktuell die Zeile „Zwei Dinge, die in der Praxis
überraschen:" gefolgt von zwei Bullet-Punkten (System-Ebene „Archicad-Ebene" +
leere Ebenen löschbar), direkt vor der Überschrift „## Ebenenkombinationen".

Ändere „Zwei Dinge" zu „Drei Dinge" und hänge nach dem zweiten Bullet
(„Leere Ebenen sind per API gefahrlos löschbar …") einen dritten Bullet an,
wortgleich mit diesem Text:

„- **`layerIndex` (den `SetDetailsOfElements` verlangt) ist weder die GUID-Reihenfolge
noch die Position in der Attributliste.** <!-- 2026-09-06 --> Beleg: A_01_TRAGWAND
hat Index 342, steht aber an Listenposition 104; A_125_DACHFLAECHENFENSTER 662/123;
Q_22_ELEKTRO 865/664. Aus einem lebenden Element ableitbar ist der Index nur, wenn
auf der Ebene bereits ein Element liegt — bei leeren Zielebenen scheitert das. Der
native Weg ist `API.GetAttributesIndices` (Antwort
`attributeIndicesAndGuids[].attributeIndexAndGuid.{guid,index}`); Tapir 1.5.4 kennt
den Befehl noch nicht (Fehler 4010). Indizes sind projektspezifisch — vor
Wiederverwendung neu holen. Live THN 2026-09-06: A_21_AUSSPAR_WAND=567,
A_22_AUSSPAR_DECKE=127, A_23_AUSSPAR_BODEN=117."

Nur diese zwei Änderungen (Wortänderung „Zwei"→„Drei" + neuer Bullet). Keine
weiteren Abschnitte der Datei anfassen.
  </action>
  <verify>
    <automated>grep -q 'Drei Dinge, die in der Praxis' reference/attribute-und-ebenen.md && grep -q '<!-- 2026-09-06 -->' reference/attribute-und-ebenen.md && grep -q 'GetAttributesIndices' reference/attribute-und-ebenen.md && grep -q 'A_21_AUSSPAR_WAND=567' reference/attribute-und-ebenen.md && echo "TASK1 OK"</automated>
  </verify>
  <done>
„## Ebenen" enthält „Drei Dinge, die in der Praxis überraschen:" und einen dritten
Bullet mit dem Datums-Marker 2026-09-06, `GetAttributesIndices` und den drei
THN-Live-Werten. Der restliche Dateiinhalt ist unverändert.
  </done>
</task>

<task type="auto">
  <name>Task 2: Geschosslogik + Beschriftung der Durchbruch-Symbole in oeffnungen-aus-konturen.md</name>
  <files>recipes/oeffnungen-aus-konturen.md</files>
  <action>
Ziel-Datei ist `recipes/oeffnungen-aus-konturen.md`, NICHT `recipes/library-objects.md`:
Diese Datei behandelt bereits die SAB-Durchbruch-Symbole („Bodendurchbruch Symbol",
„Deckendurchbruch Symbol", „Bodenschlitz") inkl. einer eigenen Anti-Pattern-Sektion
zu genau diesen Objekten (Zeile 80ff.) — library-objects.md deckt generische
Library-Objects ohne Öffnungs-Bezug ab und ist der falsche Ort.

Hänge am Dateiende (nach der letzten Zeile des Abschnitts „## ⚠️ Parser-Falle …")
einen neuen Abschnitt an:

„## Geschosslogik + Beschriftung der Durchbruch-Symbole <!-- 2026-09-06 -->

Die Objekte „Bodendurchbruch Symbol", „Deckendurchbruch Symbol" und „Bodenschlitz"
zeichnen Geschossdarstellung und Typtext SELBST: `lineTypeFloor` gilt im eigenen
Geschoss, `lineTypeCeiling` im Nachbargeschoss, und der Typtext wechselt
spiegelbildlich (Bodendurchbruch auf Geschoss n zeigt „BD", auf n−1 „DD";
Deckendurchbruch umgekehrt nach oben). Ob im Nachbargeschoss überhaupt gezeichnet
wird, hängt allein an der Elementeinstellung „Auf Geschossen zeigen" — nicht am
Skript. Per Tapir nicht setzbar; seit ELM_SAB 0.9.15 dafür
`ELM_SAB.SetStoryVisibilityOfElements` / `GetStoryVisibilityOfElements` (Befehl
+ Parameter vollständig dokumentiert in
[`../reference/mcp-extension.md`](../reference/mcp-extension.md#elm_sab-0915--auf-geschossen-zeigen),
hier nur der Fachkontext). Live verifiziert THN 2026-09-06:
`visibility: "HomeAndOneDown"` → Rücklese `showRelBelow: 1`, Sichtbeleg im
Grundriss (eigenes Geschoss durchgezogen, Geschoss darunter gestrichelt, Text
BD→DD). Die DevKit-Warnung, das Relativ-Geschoss-Feature sei für
`API_ObjectType` „not extended", trifft in AC29 damit NICHT zu.

**Beschriftung derselben Objekte:** Text = `symb_use_short` (nur wenn
`bShowPrefix`) + Typtext + `A` + „ /" + `B`. `iSymbUse`: 1 Elektro · 2 Gas ·
3 Heizung · 4 Lüftung · 5 Sanitär · 0 „Eigene" — es gibt KEIN Kälte und KEINE
Kombi-Gewerke (HS, HLS, SH, ESH …); die laufen über `iSymbUse=0` +
`bShowCustomText=true` + `symb_cust_text`. Bei String/Integer-Paaren
(`symb_use`/`iSymbUse`) führt der Integer, der String wird bei Regeneration
daraus abgeleitet. Ohne `bShowPrefix=1` erscheint das Gewerkskürzel NIE — am
THN stand es projektweit auf 0, deshalb las der Plan „BD 1,60 /0,25" ohne
Gewerk, während 992 Objekte zusätzlich pauschal auf „Lüftung" (Werksdefault)
standen."

Nur diesen einen neuen Abschnitt anhängen. Keine bestehenden Abschnitte der
Datei verändern.
  </action>
  <verify>
    <automated>grep -q '## Geschosslogik + Beschriftung der Durchbruch-Symbole' recipes/oeffnungen-aus-konturen.md && grep -q '<!-- 2026-09-06 -->' recipes/oeffnungen-aus-konturen.md && grep -q 'SetStoryVisibilityOfElements' recipes/oeffnungen-aus-konturen.md && grep -q 'iSymbUse' recipes/oeffnungen-aus-konturen.md && grep -q 'mcp-extension.md#elm_sab-0915' recipes/oeffnungen-aus-konturen.md && echo "TASK2 OK"</automated>
  </verify>
  <done>
Neuer Abschnitt „## Geschosslogik + Beschriftung der Durchbruch-Symbole" mit
Datums-Marker 2026-09-06 steht am Dateiende, verweist per Markdown-Link auf
mcp-extension.md statt den Befehl erneut zu erklären, und dokumentiert die
`iSymbUse`/`bShowPrefix`-Beschriftungsgrammatik.
  </done>
</task>

<task type="auto">
  <name>Task 3: Betriebs-Fallen in mcp-conventions.md ergänzen</name>
  <files>reference/mcp-conventions.md</files>
  <action>
Hänge am Dateiende (nach dem letzten Absatz des Abschnitts „## Verhalten bei
„nein" oder Mid-Batch-Fehler") einen neuen Abschnitt an:

„## Betriebs-Fallen (THN-Live-Sitzung) <!-- 2026-09-06 -->

- **Zeitgrenze auf 600 s setzen, auch bei „timed out" wiederholen.**
  Teamwork-Schreibungen und Senden können über 180 s dauern; die HTTP-Zeitgrenze
  im Hilfsmodul auf 600 s setzen und nicht nur bei 4001 (modaler Dialog, siehe
  oben) wiederholen, sondern auch beim reinen Timeout-Symptom „timed out" —
  beides ist vorübergehend, nicht fatal.
- **Aktionsbefehle nie mit leeren Parametern „zum Schema-Test" aufrufen — sie
  werden AUSGEFÜHRT.** `FitInWindow {}` setzt den Zoom auf „alles einpassen",
  `ChangeWindow` löscht die Selektion des Nutzers (kostete 2026-09-06 eine vom
  Nutzer markierte Objektgruppe). Schema stattdessen per Discovery lesen, nie
  live gegentesten.
- **Archicad nie per `kill` beenden.** Ein abgelehnter `osascript … to quit`
  mit `-128 „Von Benutzer:in abgebrochen"` heißt Abbruch durch den Nutzer,
  nicht Hänger. `kill -TERM` erzeugte 2026-09-06 einen Absturz mit
  Graphisoft-Bug-Reporter; folgenlos war das nur, weil kein Projekt offen war.
  Vor jedem Beenden `GetProjectInfo` je Port prüfen — bei offenem Projekt erst
  `TeamworkSend`, sonst den Nutzer selbst beenden lassen."

Nur diesen einen neuen Abschnitt anhängen. Keine bestehenden Tabellenzeilen oder
Abschnitte der Datei verändern.
  </action>
  <verify>
    <automated>grep -q '## Betriebs-Fallen (THN-Live-Sitzung)' reference/mcp-conventions.md && grep -q '<!-- 2026-09-06 -->' reference/mcp-conventions.md && grep -q '600 s' reference/mcp-conventions.md && grep -q 'kill -TERM' reference/mcp-conventions.md && grep -q 'FitInWindow' reference/mcp-conventions.md && echo "TASK3 OK"</automated>
  </verify>
  <done>
Neuer Abschnitt „## Betriebs-Fallen (THN-Live-Sitzung)" mit Datums-Marker
2026-09-06 steht am Dateiende mit den drei Regeln (Zeitgrenze/Retry,
Aktionsbefehl-Risiko, kill-Verbot).
  </done>
</task>

</tasks>

<verification>
1. Alle drei Ziel-Dateien enthalten genau einen neuen Datums-Marker
   `<!-- 2026-09-06 -->`: `grep -rc '2026-09-06' reference/attribute-und-ebenen.md
   recipes/oeffnungen-aus-konturen.md reference/mcp-conventions.md` — jeweils ≥1.
2. Kein Inhalt aus Commit 0803c3f (Parameter-Schema von
   SetStoryVisibilityOfElements) wurde in `oeffnungen-aus-konturen.md` erneut
   ausbuchstabiert — nur ein Markdown-Link auf `mcp-extension.md` steht dort.
3. `git status --porcelain` zeigt ausschließlich die drei geplanten Dateien als
   geändert, keine neuen/gelöschten Dateien.
4. Kein Build, keine Tests — reine Markdown-Ergänzungen.
</verification>

<success_criteria>
- Alle drei `<inhalte>`-Punkte aus dem Auftrag stehen an ihrem inhaltlich
  passenden Ort mit Datums-Marker 2026-09-06.
- Keine Wiederholung des in Commit 0803c3f bereits dokumentierten
  ELM_SAB-Befehlsschemas — nur Verweis.
- Nur die drei vorgesehenen Dateien wurden verändert; nichts committet, nichts
  gepusht (Executor-Aufgabe).
</success_criteria>

<output>
Nach Abschluss `.planning/quick/260906-hs0-live-erkenntnisse-thn-2026-09-06-in-den-/260906-hs0-SUMMARY.md` schreiben.
</output>
